{
  "name": "poker-staff-dashboard",
  "version": "1.0.0",
  "description": "Poker Staff Dashboard for Guest Relation Executive",
  "main": "index.js",
  "scripts": {
    "start": "node scripts/start-with-port.js",
    "start:default": "react-scripts start",
    "start:clear": "node -e \"const fs = require('fs'); const path = require('path'); const cacheFile = path.join('node_modules', '.cache', '.eslintcache'); try { fs.unlinkSync(cacheFile); } catch(e) {}\" && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "clear-cache": "node -e \"const fs = require('fs'); const path = require('path'); const cacheFile = path.join('node_modules', '.cache', '.eslintcache'); try { fs.unlinkSync(cacheFile); console.log('Cache cleared!'); } catch(e) { console.log('Cache file not found or already cleared.'); }\""
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.9.4",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.24",
    "tailwindcss": "^3.3.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
